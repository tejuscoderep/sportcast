"use client"

export function useScoreProvider() {
  // Legacy hook - now a no-op.
  // The new PlayHQ integration is managed by usePlayHQConnection hook
  // and the playhq* store fields.
  return {}
}
